stages:
  - build
  - deploy

default:
  tags:
    - orange-pi-5-4gb

build:
  stage: build

  script:
    - docker compose build

deploy:
  stage: deploy

  script:
    - docker compose up -d --build