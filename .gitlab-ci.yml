stages:
  - build
  - deploy

default:
  tags:
    - orange-pi-5

build:
  stage: build

  script:
    - /usr/bin/docker compose build

deploy:
  stage: deploy

  script:
    - docker compose up -d --build