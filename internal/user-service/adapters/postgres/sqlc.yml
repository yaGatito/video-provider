version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/query.sql"
    schema: "sql/migrations"
    gen:
      go:
        out: "db"
        package: "postgres"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: false
